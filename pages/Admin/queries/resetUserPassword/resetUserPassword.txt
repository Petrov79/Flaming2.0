UPDATE users
SET
    password = crypt(
        {{ InputNewPassword.text }},
        gen_salt('bf')
    ),
    must_change_password = true
WHERE
    id = {{ appsmith.store.editUser.id }}
RETURNING
    id;