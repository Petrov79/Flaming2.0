INSERT INTO sessions
(
    user_id,
    expires_at
)
VALUES
(
    {{ authLogin.data?.[0]?.id ?? -1 }},
    CURRENT_TIMESTAMP + interval '30 minutes'
)
RETURNING
    token,
    expires_at,
    created_at;