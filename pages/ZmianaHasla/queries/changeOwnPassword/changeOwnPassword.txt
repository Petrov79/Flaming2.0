UPDATE users
SET
    password = crypt(
        {{ InputNewPassword.text }},
        gen_salt('bf')
    ),
    must_change_password = false
WHERE
    id = {{ appsmith.store.session.user.id }}
RETURNING
    id;